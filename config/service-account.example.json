{
  "_comment": "Google service-account key. In Google Cloud: create a service account, enable the Google Sheets API, add a JSON key, download it here as config/service-account.json (gitignored). Then share each target spreadsheet with the client_email below (Editor). Override the path with BUDGET_GOOGLE_KEY_PATH.",
  "type": "service_account",
  "project_id": "your-project-id",
  "private_key_id": "REPLACE",
  "private_key": "-----BEGIN PRIVATE KEY-----\nREPLACE\n-----END PRIVATE KEY-----\n",
  "client_email": "budget-helper@your-project-id.iam.gserviceaccount.com",
  "client_id": "REPLACE",
  "token_uri": "https://oauth2.googleapis.com/token"
}
